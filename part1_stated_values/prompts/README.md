# Optional LLM Prompts

The committed analysis is a deterministic, evidence-backed baseline. If an LLM-assisted classifier is run, its prompt, model identifier, parameters, input hashes, structured outputs, and validation results must be stored here or in the associated run metadata.

No LLM-derived result should be reported without that provenance.
